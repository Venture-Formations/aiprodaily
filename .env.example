# ============================================
# AI PROFESSIONAL NEWSLETTER PLATFORM
# Environment Configuration Template
# ============================================

# Database - Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI - OpenAI
OPENAI_API_KEY=your-openai-api-key

# Email - MailerLite
MAILERLITE_API_KEY=your-mailerlite-api-key

# Cron Security
CRON_SECRET=generate-random-32-char-string

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-random-32-char-string

# Google OAuth (Required for admin authentication)
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Admin Access Control (Required in production)
# Comma-separated list of email addresses allowed to access admin dashboard
# Example: admin@example.com,user@example.com
# IMPORTANT: If this is empty or not set, all access will be BLOCKED in production
# Staging/preview environments bypass this check automatically
ALLOWED_ADMIN_EMAILS=

# Vercel API (Optional - for deployment management)
VERCEL_API_KEY=your-vercel-api-key

# Google Cloud Vision API (Optional)
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Slack Notifications (Optional)
SLACK_WEBHOOK_URL=

# GitHub Integration (Required for image uploads)
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_REPO=Venture-Formations/aiprodaily
GITHUB_BRANCH=master

# ============================================
# AI TOOLS DIRECTORY (Clerk + Stripe)
# ============================================

# Clerk Authentication (for /tools directory)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key
CLERK_SECRET_KEY=your-clerk-secret-key

# Supabase Storage Bucket (for tool images)
NEXT_PUBLIC_TOOL_IMAGES_BUCKET=tool-images

# Stripe (for sponsored listings)
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
NEXT_PUBLIC_STRIPE_PRICE_ID_MONTHLY=your-monthly-price-id
NEXT_PUBLIC_STRIPE_PRICE_ID_YEARLY=your-yearly-price-id

# Kickbox Email Verification (Optional - verifies emails before adding to mailing list)
KICKBOX_API_KEY=your-kickbox-api-key

# SparkLoop Integration
# Webhook secret for verifying webhook payloads
SPARKLOOP_WEBHOOK_SECRET=your-sparkloop-webhook-token
# API key for Upscribe recommendations (found in SparkLoop Dashboard > Settings > Integrations)
SPARKLOOP_API_KEY=your-sparkloop-api-key
# Upscribe ID (found in page header of your Upscribe settings or via API)
SPARKLOOP_UPSCRIBE_ID=your-sparkloop-upscribe-id

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file to .env.local
# 2. Fill in all required values (marked with "your-")
# 3. Generate random strings for CRON_SECRET and NEXTAUTH_SECRET
# 4. Set up Google OAuth credentials at https://console.cloud.google.com
# 5. Add allowed admin emails to ALLOWED_ADMIN_EMAILS
# 6. Create GitHub Personal Access Token with repo scope at https://github.com/settings/tokens
# 7. Deploy to Vercel and set environment variables there too
#